[CLI]
# Default render quality; override with ``manim -ql`` / ``manim -qh``.
quality = medium_quality

[CLI_CTX]
# Uncomment to set a default scenes directory when iterating locally.
# window_position = UL
